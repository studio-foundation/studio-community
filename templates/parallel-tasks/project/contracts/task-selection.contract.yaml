name: task-selection
version: 1

schema:
  required_fields:
    - tasks
  field_constraints:
    - field: tasks
      type: array
      min_items: 5
      max_items: 5
      item_fields:
        - id
        - name
        - brief
