name: task-selection
version: 1

schema:
  required_fields:
    - tasks
  fields:
    tasks:
      type: array
      items:
        type: object
        required_fields:
          - id
          - name
          - brief
