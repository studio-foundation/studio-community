name: entities
version: 1
schema:
  required_fields:
    - entities
  fields:
    entities:
      type: array
      items:
        type: object
        required_fields:
          - name
          - type
          - confidence
          - evidence
tool_calls:
  minimum: 1
  required_tools:
    - repo_manager.read_file
